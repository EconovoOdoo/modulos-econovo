<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">

    <record id="ir_cron_room_booking_recurrence_generate" model="ir.cron">
        <field name="name">Room Recurring Booking: generate upcoming occurrences</field>
        <field name="model_id" ref="model_room_booking_recurrence"/>
        <field name="state">code</field>
        <field name="code">model._cron_generate_bookings()</field>
        <field name="user_id" ref="base.user_root"/>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="active">True</field>
    </record>

</odoo>
