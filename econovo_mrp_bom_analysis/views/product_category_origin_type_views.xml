<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ====================================================================
         TREE VIEW
         ==================================================================== -->
    <record id="product_category_origin_type_view_tree" model="ir.ui.view">
        <field name="name">product.category.origin.type.view.tree</field>
        <field name="model">product.category.origin.type</field>
        <field name="arch" type="xml">
            <tree editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="code"/>
                <field name="color" widget="color_picker"/>
                <field name="description" optional="hide"/>
            </tree>
        </field>
    </record>

    <!-- ====================================================================
         FORM VIEW
         ==================================================================== -->
    <record id="product_category_origin_type_view_form" model="ir.ui.view">
        <field name="name">product.category.origin.type.view.form</field>
        <field name="model">product.category.origin.type</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="Type Name"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="code"/>
                            <field name="sequence"/>
                        </group>
                        <group>
                            <field name="color" widget="color_picker"/>
                            <field name="active"/>
                        </group>
                    </group>
                    <group>
                        <field name="description" placeholder="Description of this origin type..."/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- ====================================================================
         ACTION
         ==================================================================== -->
    <record id="product_category_origin_type_action" model="ir.actions.act_window">
        <field name="name">Origin Types</field>
        <field name="res_model">product.category.origin.type</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first origin type
            </p>
            <p>
                Origin types are used to classify product categories for BOM cost analysis.
                Examples: Raw Material, Commercial, Subassembly, Component.
            </p>
        </field>
    </record>

    <!-- ====================================================================
         MENU (under Manufacturing > Configuration)
         ==================================================================== -->
    <menuitem id="menu_product_category_origin_type"
              name="Origin Types"
              parent="mrp.menu_mrp_configuration"
              action="product_category_origin_type_action"
              sequence="50"/>

</odoo>
